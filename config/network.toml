[gcs]
# UDP telemetry is broadcast so the laptop IP can change between networks.
ip = "255.255.255.255"
telemetry_port = 14550
command_port = 14551
video_port = 5600

[telemetry]
send_interval_ms = 1000
